---
book: 第一编 佛法總學
---

# 第一编 佛法總學 · 概览仪表盘

```dataview
TABLE word_count AS "千字", dateformat(date, "yyyy.MM") AS "年代", location AS "地点"
FROM "_research/01_佛法總學"
WHERE book AND sequence
SORT sequence ASC
```

> 点击表头可排序。未来可扩展 `keywords`（关键字）和 `themes`（核心思想）列。

```dataview
TABLE category AS "子目", word_count AS "千字", dateformat(date, "yyyy.MM") AS "年代", location AS "地点", publication AS "刊载"
FROM "_research/01_佛法總學"
WHERE book AND sequence
SORT sequence ASC
```
