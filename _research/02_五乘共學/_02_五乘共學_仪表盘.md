---
book: 第二编 五乘共學
---

# 第二编 五乘共學 · 概览仪表盘

```dataview
TABLE word_count AS "千字", dateformat(date, "yyyy.MM") AS "年代", location AS "地点"
FROM "_research/02_五乘共學"
WHERE book AND sequence
SORT sequence ASC
```

> 点击表头可排序。未来可扩展 `keywords`（关键字）和 `themes`（核心思想）列。

关键词列表
```dataview
LIST
"<br>**论域**: " + domains + 
"<br>**论用**: " + functions + 
"<br>**旨归**: " + bearings +
"<br>**要义**: " + concepts
FROM "_research/02_五乘共學" 
WHERE book AND sequence AND domains
SORT sequence ASC
```